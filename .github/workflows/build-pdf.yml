name: Build and Upload PDFs

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  # ------------------------------------------------------------
  # Job 1: Extract flattened paper list from hierarchical JSON
  # ------------------------------------------------------------
  setup:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v4

      - id: set-matrix
        run: |
          # Flatten sections[].items[].slug into a JSON array
          MATRIX=$(jq -c '[.sections[].items[].slug]' papers.json)
          echo "matrix=$MATRIX" >> "$GITHUB_OUTPUT"

  # ------------------------------------------------------------
  # Job 2: Build PDFs in parallel (one job per paper)
  # ------------------------------------------------------------
  build-pdf:
    needs: setup
    runs-on: ubuntu-latest

    strategy:
      matrix:
        paper: ${{ fromJson(needs.setup.outputs.matrix) }}

    steps:
      - uses: actions/checkout@v4

      - name: Install minimal LaTeX
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            texlive-latex-recommended \
            texlive-latex-extra \
            texlive-fonts-recommended \
            texlive-science \
            texlive-bibtex-extra \
            biber \
            latexmk \
            lmodern \
            ghostscript \
            fonts-freefont-otf \
            fonts-lmodern

      - name: Compile LaTeX to PDF
        working-directory: papers/${{ matrix.paper }}
        run: |
          set -e

          mkdir -p "$GITHUB_WORKSPACE/pdf-output"

          find . -name "*.tex" ! -name "_*" | while read -r texfile; do
            echo "Compiling $texfile..."

            dir_path=$(dirname "$texfile")
            base_name=$(basename "$texfile" .tex)

            cd "$dir_path"

            latexmk -pdf -interaction=nonstopmode -halt-on-error "$(basename "$texfile")"

            if [ "$dir_path" = "." ]; then
              dest_name="$base_name.pdf"
            else
              clean_dir=$(echo "$dir_path" | sed 's|^\./||; s|/|-|g')
              dest_name="$clean_dir-$base_name.pdf"
            fi

            cp "$base_name.pdf" "$GITHUB_WORKSPACE/pdf-output/$dest_name"

            cd - > /dev/null
          done

      - name: Upload PDF artifacts
        uses: actions/upload-artifact@v4
        with:
          # Must match what build-pages.yml expects
          name: pdf-${{ matrix.paper }}
          path: pdf-output/*.pdf
          retention-days: 1
