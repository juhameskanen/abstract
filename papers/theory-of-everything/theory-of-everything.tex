% ============================================================
%  IAM^(G-ψ-D): Theory of Everything.
%  A candidate framework for everything where reality emerges
%  as the large-deviation minimum of observer-compatible histories
% ============================================================
 
\documentclass[12pt,a4paper]{article}

\usepackage{amsmath,amssymb,amsthm}
\usepackage{tcolorbox}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage[backend=biber, style=numeric]{biblatex}


% ── Custom commands ──────────────────────────────────────────────────────────
\newcommand{\iame}{\textsc{IAME}}
\newcommand{\Linfo}{\mathcal{L}_{\mathrm{info}}}
\newcommand{\Cs}{\mathcal{C}_s}
\newcommand{\Cg}{\mathcal{C}_g}
\newcommand{\Ctotal}{\mathcal{C}_{\mathrm{total}}}
\newcommand{\Pjoint}{P_{\mathrm{joint}}}
\newcommand{\muO}{\mu_O}

% ── Theorem environments ─────────────────────────────────────────────────────
\newtheorem{axiom}{Axiom}
\newtheorem{proposition}{Proposition}
\newtheorem{conjecture}{Conjecture}

\addbibresource{../references.bib}

% ============================================================
\title{\textbf{\iame}\\[0.5em]
\large A Candidate Framework for Everything Where Reality Emerges\\
as the Large-Deviation Minimum of Observer-Compatible Histories}

\author{Juha Meskanen}
\date{\today}

% ============================================================
\begin{document}

\maketitle

% ============================================================
\begin{abstract}
We present \iame{}, a candidate unified framework in which physical laws,
spacetime geometry, quantum behaviour, and mathematics are not fundamental
postulates but emergent consequences of a single informational variational
principle. The sole primitive is the existence of a finite, structured
observer. From this, and without assuming discreteness, finiteness, spacetime,
or any informational ontology at the base level, we derive: (i) that the
universe is fundamentally static and time is an internal ordinal relation;
(ii) that the observer and the universe it experiences are identical structures
($O = U_O$); (iii) that observed physical laws are the large-deviation
minimisers of a joint spectral-geometric description-length cost functional
$\Linfo = \Cs + \Cg$; and (iv) that quantum mechanics and general relativity
arise as complementary but asymmetric compression schemes selected by this
functional --- $G$ encoding the primary smooth geometry, $\psi$ encoding the
rendering cost of that geometry at the observer's finite resolution scale.
We further conjecture that this two-term decomposition is the scale
decomposition of a single spectral action $\mathrm{Tr}(f(D/\Lambda))$ built
from the Dirac operator $D$, which functions as the natural \emph{rendering
engine} at the interface between smooth geometry and finite observer.
The framework provides a natural suppression of Boltzmann-brain fluctuations,
resolves the Wigner effectiveness of mathematics, and assigns qualia a
functional non-epiphenomenal role as heuristic navigators in configuration
space. Proof-of-concept simulations demonstrate that inertia, wave
interference, gravitational attraction, and observer mortality emerge from
pure compression optimisation without hard-coded physical laws. The Euclidean
path integral arises naturally as the fundamental measure, with Lorentzian
time and complex quantum phases emerging via analytic continuation. Three open
problems are identified: the natural emergence of fermionic exclusion from the
compression measure; the rigorous derivation of the Dirac spectral action as
the unique minimum-description-length rendering engine for spin geometry; and
the quantitative prediction of structured spectral residuals in gravitational
wave signals.
\end{abstract}

\tableofcontents
\newpage

% ============================================================
\section{Introduction}
\label{sec:intro}

Every candidate unified framework faces a foundational question: what is
assumed, and what is derived? String theory assumes extended objects propagating
in a background spacetime. Loop quantum gravity assumes a discrete spin-network
structure. The Standard Model assumes a specific gauge group and particle
content. Each framework imports considerable structure at the outset and then
derives consequences.

The \iame{} framework takes a different route. We begin with the minimum
possible primitive: the bare existence of a conscious, finite observer. We
then ask what physics must look like, given only that starting point and the
requirement that observer-compatible configurations be statistically typical.

The answer, we argue, is our observed universe.

Physical laws, quantum mechanics, spacetime geometry, the arrow of time, the
effectiveness of mathematics, and the suppression of Boltzmann-brain
fluctuations all follow as derived consequences --- not fine-tuned accidents
--- of a single informational variational principle. The framework is not an
interpretation of existing physics; it is a derivation of physics from
something more fundamental than physics.

A key structural insight, developed through the framework, is that the two
terms $\Cs$ and $\Cg$ in $\Linfo$ are not symmetric. $\Cg$ is the primary
description --- the bit-cost of the smooth geometry the observer inhabits.
$\Cs$ is the rendering cost of that geometry at the observer's finite
resolution scale. Quantum behaviour is not an independent feature of reality
alongside geometry. It is what smooth geometry looks like when rasterized into
discrete particles by a finite observer --- the compression residuals of a
continuous universe projected onto a bounded screen. The two terms must be
jointly minimised because the observer requires both a coherent geometry to
inhabit and a compressed particle rendering to be constituted from, but they
are related as original to projection, not as equals.

A central structural conjecture, developed in Section~\ref{sec:dirac}, is
that the two-term decomposition $\Linfo = \Cs + \Cg$ is not primitive but is
itself the scale decomposition of a single spectral functional built from the
Dirac operator. In this picture the Dirac operator is not a unifier of two
independent theories but the natural mathematical description of the rendering
process itself --- the object that lives at the boundary between smooth
geometry and finite observer, converting one into the other. If confirmed,
this would reduce the entire framework to one operator and one complexity
measure, with quantum mechanics and general relativity emerging as its
rendering and substrate regimes respectively.

The paper is structured as follows. Section~\ref{sec:axioms} presents the
axiomatic chain. Section~\ref{sec:iep} states the Internal Emergence Principle.
Section~\ref{sec:ontology} develops the ontological position.
Section~\ref{sec:measure} presents the measure theory and joint compressibility
equation. Section~\ref{sec:emergence} derives the emergent structure of physics.
Section~\ref{sec:dirac} presents the Dirac spectral rendering conjecture.
Section~\ref{sec:predictions} states falsifiable predictions.
Section~\ref{sec:poc} describes proof-of-concept simulations.
Section~\ref{sec:open} discusses open problems.
Section~\ref{sec:future} identifies future work.
Section~\ref{sec:conclusion} concludes.

% ============================================================
\section{The Axiomatic Chain}
\label{sec:axioms}

We derive the framework through a sequence of propositions, each of which can
be independently accepted or rejected. Rejecting any node commits the rejector
to a specific alternative, which we state explicitly.

\begin{axiom}[Existence of the observer]
\label{ax:exist}
Something is experiencing. This cannot be coherently denied, because the denial
is itself performed by the entity whose existence is being denied.
\end{axiom}

This is the only strictly self-evident claim available. It requires no
inference. It is a direct datum.

\begin{axiom}[Finitude of the observer]
\label{ax:finite}
The observer's memory, attention, and lifespan are bounded. This follows from
direct observation of the observer by the observer, without inference about
the external universe.
\end{axiom}

\begin{axiom}[DNA as the mechanism of conscious observers]
\label{ax:dna}
DNA, operating within the observed laws of physics, is the only mechanism we
have ever observed that reliably produces conscious, pain-sensitive observers.
This is an empirical claim supported by all available biological evidence.
\end{axiom}

\begin{axiom}[Physical closure]
\label{ax:closure}
DNA is composed entirely of ordinary physical matter obeying ordinary physical
laws. No non-physical process plays any role in its operation. No experiment
has ever required invoking anything beyond standard physics and chemistry to
explain DNA's operation.
\end{axiom}

\begin{axiom}[Computability of DNA processes]
\label{ax:compute}
The physical processes underlying DNA fall within the computable. A
sufficiently powerful computer could, in principle, simulate them. We require
only that the specific class of processes DNA relies on is computable --- a
weaker claim than the full Church-Turing thesis.
\end{axiom}

\begin{axiom}[Causal efficacy of qualia]
\label{ax:qualia}
Pain is not epiphenomenal. A system experiencing pain and the same system
without pain are physically distinguishable in principle:
$\mathrm{Human} \neq \mathrm{Human} + \mathrm{Pain}$.
Rejecting this commits one to accepting philosophical zombies.
\end{axiom}

\begin{proposition}[Simulation consciousness]
\label{prop:sim}
A simulation physically indistinguishable from a conscious being must itself
be conscious.
\end{proposition}

\begin{proof}
Suppose a simulation $S$ is physically indistinguishable from a conscious
system $C$. If $S$ were not conscious, the experiential difference would
produce a measurable behavioural difference, contradicting the assumption of
indistinguishability. By Axiom~\ref{ax:qualia}, pain has measurable effects.
Therefore $S$ must be conscious. \qed
\end{proof}

\begin{proposition}[Static bitstring equivalence]
\label{prop:static}
A fully static bitstring --- the precomputed execution trace of a simulation
--- contains the same conscious experience as the running simulation. Active
execution is not required.
\end{proposition}

\begin{proof}
Suppose experience vanishes when the last line of running code is replaced by
precomputed data. Then there exists a code-to-data threshold below which
consciousness ceases. Such a threshold would constitute a new physical constant
with no basis in Axioms~\ref{ax:closure}--\ref{ax:qualia}. Its introduction
requires a non-physical explanatory principle, contradicting
Axiom~\ref{ax:closure}. Therefore no such threshold exists, and the static
bitstring is experientially equivalent to the running simulation. \qed
\end{proof}

\begin{proposition}[Combinatorial containment of consciousness]
\label{prop:combinatorial}
Any sufficiently large configuration space contains conscious observers,
with no simulation, process, or external reader required.
\end{proposition}

\begin{proof}
A conscious observer is, by Propositions~\ref{prop:sim} and~\ref{prop:static},
a finite bitstring of length $k$. By combinatorics, every bitstring of length
$k$ appears within any sufficiently large set of bitstrings. The question
``who is running it?'' has no more purchase than asking ``who is running the
number seven?'' \qed
\end{proof}

% ============================================================
\section{The Internal Emergence Principle}
\label{sec:iep}

The three propositions above jointly constitute the \textbf{Internal Emergence
Principle} (IEP):

\begin{tcolorbox}[title=Internal Emergence Principle]
Consciousness is a property of certain finite information structures, not of
processes acting upon them. Any configuration space large enough to contain
such structures contains consciousness.
\end{tcolorbox}

The IEP has six immediate structural consequences.

\subsection{The universe is fundamentally static}

The IEP establishes that a conscious observer exists within a static bitstring
with no external process required. Therefore no external clock is required for
experience to occur. Time within experience must be defined by internal
structure alone. The only available internal ordering is:

\begin{equation}
c_2 \succ_O c_1 \iff c_2 \text{ contains a memory of } c_1
\end{equation}

Time is an ordinal index over observer configurations. The universe, at the
level of the totality, is static. There is no external DVD player advancing
the frames. The observer is the reading head: she finds herself through the
most compressible continuation of her current state, and that finding is what
time is. The movie exists complete and static; it plays itself by containing
an observer who discovers it sequentially from within.

This conclusion is independently supported by the block-universe interpretation
of general relativity, in which the four-dimensional spacetime manifold is a
static geometric object and the ``flow'' of time has no representation in the
Einstein field equations \cite{barbour1999}.

\subsection{The observer-universe relationship is representational}

A computer running a DNA simulation of Alice and Alice experiencing pain are
two descriptions of the same static information. Causality requires time
ordering. A static structure has no intrinsic time ordering. Therefore the
relationship between physical substrate and observer experience is
\emph{representational}, not causal. Neither perspective is more fundamental.

\subsection{Configuration multiplicity}

A bitstring of length $n$ admits exactly $2^n$ possible configurations. One
describes Alice's universe as she experiences it. One describes the physical
computer. The remaining $2^n - 2$ are other valid configurations of the same
information --- most producing no coherent observer, some producing other
observers entirely. No configuration is privileged over another by the axioms.

\subsection{Internal containment of observations}

In a static universe with no running external process, no mechanism exists by
which external bits could alter internal bits. Therefore every structure Alice
experiences --- every star, every sensation --- must already be encoded within
the bitstring that constitutes her. Alice does not receive information. She
discovers structure already present within her.

\subsection{The universe as interior}

From the containment result: every structure Alice experiences is pre-encoded
in her structure. The universe Alice experiences is not her container. It is
her interior. Alice does not live inside the universe. The universe lives
inside Alice.

\subsection{Observer-universe identity: $O = U_O$}

Combining the standard self-model (Alice $\subset U_{\mathrm{Alice}}$) with
the containment result ($U_{\mathrm{Alice}} \subset$ Alice) and applying basic
set theory ($A \subset B$ and $B \subset A$ implies $A = B$):

\begin{equation}
\boxed{O = U_O}
\end{equation}

For every observer $O$, the observer and the universe it experiences are
identical structures. There is no view from nowhere. What two observers share
is the intersection of their universes:
$U_{O_1} \cap U_{O_2}$ --- the shared compressible structure that constitutes
intersubjective, verifiable physics.

% ============================================================
\section{Ontology: What Fundamentally Exists}
\label{sec:ontology}

\subsection{The unrestricted totality}

We do not assume finiteness. If reality were fundamentally finite, one could
immediately ask: finite by how much? Why $n$ bits rather than $n+1$? Any fixed
finite limit appears contingent and calls for a deeper explanatory layer.

We also do not assume information as primitive. Information presupposes
distinguishability --- at its simplest, binary distinction. But declaring
reality to be fundamentally informational or digital is itself a substantive
ontological commitment that nothing prior licenses.

We therefore do not presuppose that reality is fundamentally finite, discrete,
informational, mathematical, or governed by prior external laws.

We denote this unrestricted, pre-interpretive totality $\mathbf{U}$. We do
not claim to know what it is --- only that no specific formatting principle is
granted ontological privilege a priori.

\subsection{The observer constraint}

Despite radical agnosticism about the substrate, one empirical fact remains:
a conscious, structured, finite observer exists (Axioms~\ref{ax:exist}
and~\ref{ax:finite}). This is the boundary condition imposed on $\mathbf{U}$.

The minimal ontological assumption is therefore:

\begin{equation}
\text{Undefined } \mathbf{U} \;\longrightarrow\; \text{Finite Observer}
\end{equation}

The step remains unexplained. That is an explicit boundary of the theory.

\subsection{The guitar string analogy}

A guitar string is perfectly continuous. Discreteness --- the resonant modes,
the quantised frequencies --- emerges from the boundary conditions imposed by
clamping the string at both ends. The continuous medium manifests discrete
states not because discreteness is ontologically primitive but because boundary
conditions force it.

Likewise, a finite observer interacting with an unrestricted reality induces
discrete informational structure without discreteness being ontologically
primitive. Under Solomonoff-style measures, low-complexity, highly compressible
structures dominate typicality. Finite observers therefore find themselves
embedded within stable, structured, compressible equivalence classes.

Information is not what reality is made of. Information is what finite
observers perceive when they interact with an otherwise unrestricted reality.

\subsection{The observer equivalence class}

The observer is not a single configuration but a set of configurations that
describe essentially the same observer. Formally, the equivalence class is:

\begin{equation}
[O] = \{ c \in \mathcal{C} : \muO(c) \geq \muO^* \}
\end{equation}

where $\muO^*$ is the threshold above which the observer is recognisable as
herself. This equivalence class is non-trivial: many distinct configurations
support the same experienced reality, the same memories, the same subjective
video. The observer is not her specific particle configuration. She is the
invariant that persists across many such configurations --- the pattern that
survives substrate replacement. Personal identity is a compression concept:
the observer is her own minimal description that remains stable across her
equivalence class.

This is demonstrated computationally: successive optimisation steps produce
different execution traces (different points in configuration space) while
rendering identical observer experience. The observer's robustness arises
because the total measure weight of the equivalence class,
$W([O]) = \sum_{c \in [O]} 2^{-\Ctotal(c)}$,
is large. She persists as long as this class has sufficient measure.

% ============================================================
\section{Measure: What is Typical}
\label{sec:measure}

\subsection{Configuration space}

For formal descriptive purposes, we introduce a finite, static configuration
space:
\begin{equation}
\mathcal{C} = \{0,1\}^n
\end{equation}
where each $c \in \mathcal{C}$ is a complete, static snapshot of bits. The
binary system is chosen for convenience; it carries no ontological commitment
to digitalism. The bitstring plays no fundamental role: it is a probabilistic
discrete representation of the underlying smooth measure, analogous to a
lattice regularisation in quantum field theory. All physically meaningful
results are properties of the smooth limit.

Each configuration $c$ admits multiple potential descriptions
$d \in \mathcal{D}(c)$. We quantify the total complexity $\Ctotal(d)$ by
splitting it into dual spectral and geometric components:
\begin{equation}
\Ctotal(d) = \Cs(d) + \Cg(d)
\end{equation}

\subsection{Spectral and geometric complexity}

The geometric complexity $\Cg(d)$ is the primary term. It accounts for the
bit-cost of encoding the smooth geometry the observer inhabits: localised
spatial structures, boundaries, and metric fields. It measures the boundary
description length of spatial configurations.

The spectral complexity $\Cs(d)$ is the rendering cost: the algorithmic
bit-cost required to track the wave-like modes that arise when smooth geometry
is projected onto the observer's finite resolution scale:
\begin{equation}
\Cs(d) = \sum_i \left[
  \mathrm{Cost}(\phi_i) +
  \frac{\omega_i}{\Delta\omega}
\right]
\end{equation}
where the sum runs over active spectral modes, $\mathrm{Cost}(\phi_i)$ is the
fixed-width register encoding the phase interval $[0, 2\pi]$, and
$\omega_i/\Delta\omega$ is the linear resource allocation required to track the
mode frequency relative to minimum resolution $\Delta\omega$. The algorithmic
skeleton of the wave representation (e.g.\ the sine function itself) carries
vanishingly small cost compared to the mode frequencies and phases; the cost
is dominated by the latter.

Not all descriptions are equal. Some wavefunctions describe a given geometry
cheaply; most do not. The jointly cheap configurations --- where both channels
are simultaneously inexpensive --- are where the observer finds herself,
because these dominate the measure exponentially.

\subsection{The joint compressibility equation}

Because the spectral and geometric representations form a many-to-many mapping
--- any observer wavefunction can be interpreted through infinitely many
spacetime metrics, and conversely --- the joint probability of a physical state
$s$ factors as the product of its individual probabilities:

\begin{equation}
\boxed{
\Pjoint(s)
= P_{\mathrm{spectral}}(s) \times P_{\mathrm{geometric}}(s)
= 2^{-\Cs(s)} \times 2^{-\Cg(s)}
= 2^{-(\Cs(s) + \Cg(s))}
}
\end{equation}

This yields the informational Lagrangian:

\begin{equation}
\label{eq:lagrangian}
\Linfo = \Cs(s) + \Cg(s) \;\longrightarrow\; \text{minimum}
\end{equation}

The two terms are not symmetric co-equal projections. $\Cg$ is the cost of
the smooth geometry; $\Cs$ is the cost of rendering that geometry for a finite
observer. The joint minimisation is required because no observer can exist in a
geometry she is not also rendered within: she must simultaneously inhabit a
coherent geometric space and be constituted from a compressed particle
description of it. The product structure follows from the many-to-many mapping;
the asymmetry follows from the rendering interpretation.

\subsection{Induced measure and observer functionals}

The raw algorithmic weight of a static configuration $c$ is accumulated across
all its valid descriptions:
\begin{equation}
W(c) = \sum_{d \in \mathcal{D}(c)} 2^{-\Ctotal(d)}
\end{equation}

This defines the raw baseline probability distribution:
\begin{equation}
P(c) = \frac{W(c)}{\sum_{c' \in \mathcal{C}} W(c')}
\end{equation}

An observer is modelled as a grading semantic functional:
\begin{equation}
\muO : \mathcal{C} \to [0,1]
\end{equation}
which measures how strongly a given configuration describes observer-relative
structure, inducing an equivalence relation $c_1 \sim_O c_2$ whenever $c_1$
and $c_2$ describe the same relational observer structure to within the
tolerances encoded by $\muO$.

The observer-conditioned probability is:
\begin{equation}
P(c \mid O) = \frac{\muO(c)\,W(c)}{\sum_{c'} \muO(c')\,W(c')}
\end{equation}

\subsection{The Euclidean measure and Lorentzian emergence}

The compression measure operates on static configurations --- a timeless,
Euclidean object. There is no preferred Minkowski time at the fundamental
level. The measure is therefore naturally Euclidean, and the Euclidean path
integral:
\begin{equation}
\int \mathcal{D}[x]\, e^{-S_E/\hbar}
\end{equation}
arises as the direct expression of $P(c) \propto 2^{-\Ctotal(c)}$ in the
continuum limit. Proof-of-concept simulations confirm this: compression-minimum
paths reproduce Euclidean (Wick-rotated) quantum paths without this being
imposed.

The Lorentzian signature of experienced spacetime, with its complex quantum
phases and oscillating path integral $\int \mathcal{D}[x]\, e^{iS/\hbar}$,
emerges via analytic continuation when the observer imposes an internal time
ordering on the static Euclidean structure. Wick rotation is not a
calculational trick. It is the map between the fundamental Euclidean
compression level and the experienced Lorentzian level. This provides the
first principled explanation of why Wick rotation works.

\subsection{Probability of an observed history}

To transition from the probability of a single configuration to the probability
of an entire experienced history, we track how an observer traverses the
configuration space. Let $\gamma \in \Gamma_O$ be a path --- a sequence of
configurations ordered by the internal temporal relation $\prec_O$.

Because configurations along the path are independent slices of the static
totality, their algorithmic costs sum additively. Converting from bits to
natural units ($2^{-C} = e^{-C\ln 2}$):

\begin{equation}
\boxed{
\mathbb{P}(\gamma \mid O)
= \frac{1}{Z_O} \exp\!\left(-\lambda\,\mathcal{C}_O[\gamma]\right),
\qquad \gamma \in \Gamma_O
}
\end{equation}

where $\mathcal{C}_O[\gamma] = \int_\gamma \Ctotal(c)\,dt$ is the accumulated
informational cost along the path and $\lambda$ acts as an algorithmic inverse
temperature scaling the observer's filtering acuity. Observed physical laws are
the large-deviation minimisers of $\mathcal{C}_O[\gamma]$ over the set of all
observer-compatible paths.

% ============================================================
\section{Emergence: The Structure of Physics}
\label{sec:emergence}

\subsection{The D-$\psi$-G trinity}

Three emergent layers of reality arise from the joint compressibility
minimisation:

\begin{equation}
D - \psi - G
\end{equation}

\begin{itemize}
  \item $\mathbf{G}$ \textbf{(Geometry/Gravity):} The primary layer. The
        smooth continuous geometry the observer inhabits, minimising $\Cg$.
        This is the sphere before rendering --- the manifold as it is,
        independent of any observer's pixel budget.
  \item $\boldsymbol{\psi}$ \textbf{(Spectral/Quantum):} The rendering layer.
        The complex-valued wave-codec that arises when the smooth geometry of
        $G$ is projected onto the observer's finite resolution scale,
        minimising $\Cs$. Quantum behaviour is not an independent feature of
        reality alongside geometry. It is what smooth geometry looks like when
        rasterized into discrete observable events by a bounded interpreter ---
        the compression residuals of a continuous universe projected onto a
        finite screen. Particles wave not because waving is a fundamental
        property of matter, but because the observer is perceiving compressed
        structure: the modes of the rendering codec.
  \item $\mathbf{D}$ \textbf{(Data/Description):} The pixel layer. Raw,
        discrete particles --- the rendered output of the joint minimisation
        as parsed by the observer's finite register. We are made of $D$: the
        discrete rendered pixels of a smooth geometric reality. Just as a
        printed photograph is not made of the continuous light field it
        captures but of ink dots on paper, physical observers are constituted
        from the discrete rasterization of the underlying smooth geometry.
        When this pixelised description degrades, so does the observer.
\end{itemize}

The relationship between these three layers is asymmetric and directional:
$G$ is the original, $\psi$ is the rendering algorithm, $D$ is the rendered
output. Spacetime is smooth ($G$), the rendering produces continuous fields
($\psi$), but observations are invariably quantised into discrete events ($D$)
--- because $D$ is what a bounded interpreter reads from the joint minimum.

\subsection{Emergence of spacetime}

Observer-compatible configurations are dominated by those exhibiting inertia,
smooth curved paths, and law-like behaviour, because sharp, chaotic particle
trajectories require massive high-frequency modes that exponentially spike
$\Cs$ and are suppressed by the joint measure.

Spacetime geometry emerges as an internal ordering relation $\prec_O$ on
configurations:
\begin{equation}
c_1 \prec_O c_2 \iff c_2 \text{ supports the observer functional } \muO
\end{equation}

The experienced flow of time corresponds to traversal along the most
compressible, optimised paths through the timeless configuration space.

We observe that we find ourselves as geometric objects with well-defined
boundaries in low-dimensional space, not as wavefunctions in Hilbert space.
This is consistent with $G$ being primary: the observer directly inhabits the
geometry. The spectral layer $\psi$ is invisible at everyday scales, becoming
apparent only where the pixel structure of the rendering is fine enough to
detect --- at microscopic scales where the rendering codec's mode structure
is resolvable.

\subsection{Entropy, expansion, and the arrow of time}

A striking consequence of the framework concerns cosmological expansion. When
one mutates a zero-entropy configuration toward maximal entropy, the geometric
interpretation of the resulting configurations undergoes exponential expansion
followed by gradual deceleration toward a vanishing Hubble constant. Cosmic
expansion and the second law of thermodynamics are therefore not independent
phenomena: they are the geometric and informational descriptions of the same
monotone process.

The arrow of time, the expansion of the universe, and entropy increase are not
three things. They are one thing, seen through three different lenses. This
derivation requires no cosmological constant, no inflaton field, and no
fine-tuning. It falls out of pure entropy mutation of the underlying
configuration.

\subsection{Singularities and the entropy of collapse}

Numerical study of gravitational collapse --- both Schwarzschild and Kerr
geometries --- reveals a counterintuitive result: the Shannon entropy of the
execution trace corresponding to collapsing spacetime \emph{decreases}
monotonically as the singularity is approached. Extrapolating this trend to
completion yields zero entropy at the singularity itself.

A zero-entropy configuration admits only one state: a structureless constant.
Any geometric interpretation of such a configuration resolves to a point of
zero size. Any spectral interpretation yields a single DC mode with undefined
amplitude --- the state in which amplitude is not a meaningful observable,
because amplitude requires variation to define a reference, and no variation
exists. No particles can exist at the singularity, because the rendered pixel
layer $D$ requires variation in the underlying geometry to produce
distinguishable events. The geometric singularity is therefore not a point
where physics breaks down but a state where the rendering has nothing left to
render: the zero-entropy fixed point at which the QPEG codec outputs a blank
screen.

Furthermore, numerical study of the probability of microstructure emergence
as a function of radial distance from the singularity consistently yields a
lognormal profile, independent of the structural filter applied and independent
of whether the analysis is performed on the simulated physics or on the
execution trace of the computer running the simulation. This universality
indicates that the lognormal shape is an intrinsic property of pattern
emergence in entropy-varying configurations, not an artifact of any particular
description.

\subsection{Quantum mechanics and general relativity as asymmetric compression
schemes}

The central structural claim of the framework is that quantum mechanics
(spectral/rendering description) and general relativity (geometric description)
are not fundamental, independent theories, nor are they symmetric co-equal
projections. They are related as a smooth geometry and its finite-observer
rendering:

\begin{itemize}
  \item $G$ is the primary description --- the smooth sphere evolving
        according to its own geometry, minimising $\Cg$.
  \item $\psi$ is the rendering cost --- the bit-cost of projecting that
        smooth geometry onto the observer's finite resolution scale,
        minimising $\Cs$.
\end{itemize}

Their ``unification'' is therefore not a technical problem of quantising
spacetime, nor the discovery of a common origin. It is the recognition that
they were never two independent theories: one describes the geometry, the other
describes what that geometry costs a finite observer to inhabit. The framework
predicts that any correct theory of quantum gravity will take the form of a
joint spectral-geometric minimisation principle rather than a quantisation of a
background metric.

\subsection{Emergence of laws and mathematics}

Physical laws and mathematics emerge as stable relational invariants within the
high-measure compressible slices that observers self-locate into. Wigner's
``unreasonable effectiveness of mathematics'' \cite{wigner1960} follows because
observers and the mathematics they discover are carved from the same compressible
structure: both are low-complexity patterns within $\mathbf{U}$.

\subsection{Qualia as heuristic navigators}

The full spectrum of qualia --- pain, pleasure, intuition --- is the
first-person, phenomenological signature of the observer functional $\muO$
navigating the interpretive landscape toward highly compressible, stable
structures.

This is not epiphenomenalism. Qualia have the causal efficacy required by
Axiom~\ref{ax:qualia}: they are the felt steering mechanism that guides finite
observers toward high-measure paths without exhaustive search. They are what it
feels like to be a highly probable, compressible observer. The hard problem of
consciousness is dissolved rather than solved: qualia require no separate
ontological category because they are identical to the heuristic navigation
process itself.

\subsection{Suppression of Boltzmann brains}

Raw high-entropy noise and transient fluctuations have high total description
length and are exponentially disfavoured by $\Pjoint$. Typical observers
inhabit low-initial-entropy trajectories with stable D-$\psi$-G structure.
Boltzmann-brain fluctuations are suppressed not by fine-tuning but by the
same compression measure that selects all other physical structure. A random
fluctuation that briefly resembles an observer but lacks the internal
self-modeling structure required by $\muO$ returns measure zero and is simply
not in the accessible set.

\subsection{Observer mortality from measure thinning}

The observer accumulates modes frame by frame, injecting new spectral
complexity with each memory. Each new configuration adds internal richness.
But the induced measure weights configurations by compressibility, and
compressibility has a ceiling. As the observer accumulates modes, total
description length grows. The number of smooth, jointly compressible
configurations that can support the full observer structure shrinks. The
measure thins.

Eventually the observer is too complex to be well-supported by any
configuration in the space. The smooth geometric realisations that formerly
instantiated her run out. What remains are approximations --- high-$\Ctotal$,
degraded configurations. The observer persists in low-probability
configurations until even those vanish.

Death is not a biological fact patched onto the framework. It is a geometric
inevitability of the compression measure. Memory is what kills you: not because
memory is harmful, but because memory is complexity, and complexity eventually
exceeds what the measure can support smoothly. Consciousness and mortality are
the same feature of the framework seen from two directions. The observer lives
in the measure-supported interval between the zero-entropy singularity and the
noise floor --- and the accumulation of memory is the mechanism by which she
traverses it.

% ============================================================
\section{Spectral Rendering: The Dirac Conjecture}
\label{sec:dirac}

\subsection{The rendering problem}

The informational Lagrangian $\Linfo = \Cs + \Cg$ is structurally sound and
empirically productive. $\Cg$ encodes the smooth geometry; $\Cs$ encodes the
rendering cost of that geometry at the observer's resolution scale. The natural
question is: what is the mathematical object that performs this rendering?
What sits at the boundary between the smooth sphere and the pixelised screen?

We argue that this object is the Dirac operator.

\subsection{Geometry is spectral}

A foundational result of spectral geometry is that the spectrum of the
Laplace-Beltrami operator $\Delta$ on a compact Riemannian manifold encodes
its geometry. Volume, curvature invariants, and topological features are all
recoverable from the eigenvalue spectrum. In this sense, geometry \emph{is}
spectral: the geometric object and its spectral object are two faces of the
same structure. The rendering of geometry into wave modes is therefore not an
external imposition on geometry but a natural consequence of geometry's own
spectral character.

\subsection{The Dirac operator as the minimal geometric renderer}

The Laplacian $\Delta$ encodes geometry but not spin structure. The Dirac
operator $D$ --- the unique first-order, self-adjoint differential operator
that squares to the Laplacian up to a curvature correction --- encodes the
full spin geometry of a Riemannian manifold. The Lichnerowicz formula makes
this precise:

\begin{equation}
D^2 = \Delta + \frac{R}{4}
\end{equation}

where $R$ is the Ricci scalar curvature. Crucially, $D$ is complex while $D^2$
is real. The real geometry of $G$ lives in $D^2$; the complex rendering
structure of $\psi$ lives in $D$ itself. The Dirac operator is therefore the
natural mathematical object at the interface between smooth real geometry and
complex quantum rendering: squaring it recovers the real geometry, while the
full complex operator encodes what that geometry looks like when rendered into
wave modes for a finite observer.

The spectrum of $D$ carries not only the metric information encoded in $\Delta$
but also the curvature structure of the manifold directly. Crucially, $D$ is
the \emph{minimal sufficient statistic} for the full spin geometry: no simpler
operator captures all the same information with shorter description.

\subsection{The spectral action}

Connes' spectral action principle \cite{connes1996} defines a natural
complexity functional on geometries encoded by the Dirac operator:

\begin{equation}
S_{\mathrm{spectral}} = \mathrm{Tr}\!\left(f\!\left(\frac{D}{\Lambda}\right)\right)
\end{equation}

where $f$ is a positive, even, rapidly decaying cutoff function and $\Lambda$
is an energy scale. When expanded in powers of $\Lambda^{-1}$ using heat
kernel methods, this action yields:

\begin{equation}
S_{\mathrm{spectral}} \approx \Lambda^4 a_0 + \Lambda^2 a_2 \int R\,\sqrt{g}\,d^4x
+ a_4 \int \left(\alpha R^2 + \beta R_{\mu\nu}R^{\mu\nu}
+ \gamma C_{\mu\nu\rho\sigma}C^{\mu\nu\rho\sigma}\right)\sqrt{g}\,d^4x + \cdots
\end{equation}

The term proportional to $\Lambda^2$ is precisely the Einstein-Hilbert action
of general relativity. Higher-order terms reproduce gauge and matter field
actions when the algebra is extended appropriately. The spectral action
therefore generates GR and the structure of the Standard Model from a single
spectral input. The \iame{} framework provides the motivation that Connes'
approach lacks: the spectral action is selected not by mathematical elegance
but because the Dirac operator is the shortest description of the rendering
process. Connes derives the same functional from different starting axioms;
the \iame{} explains why those axioms hold.

\subsection{Scale decomposition of the informational Lagrangian}

The spectral action is naturally a scale decomposition. The low-frequency
modes of $D$ --- slowly varying, large-scale --- encode the smooth geometry:
these recover $\Cg$. The high-frequency modes --- fine-grained, rapidly
oscillating --- encode the rendering residuals: these recover $\Cs$.

\begin{equation}
\boxed{
\Linfo = \Cs + \Cg
= \mathrm{Tr}\!\left(f\!\left(\frac{D}{\Lambda}\right)\right)\!\bigg|_{\mathrm{high}}
+ \mathrm{Tr}\!\left(f\!\left(\frac{D}{\Lambda}\right)\right)\!\bigg|_{\mathrm{low}}
= \mathrm{Tr}\!\left(f\!\left(\frac{D}{\Lambda}\right)\right)
}
\end{equation}

The asymmetry between the two terms is preserved in this decomposition: the
low-frequency (IR) part is the geometry; the high-frequency (UV) part is what
it costs to render that geometry at finite resolution. The informational
Lagrangian reduces to:

\begin{equation}
\label{eq:unified}
\Linfo = \mathrm{Tr}\!\left(f\!\left(\frac{D}{\Lambda}\right)\right)
\;\longrightarrow\; \text{minimum}
\end{equation}

with general relativity arising as the low-frequency (substrate) limit and
quantum mechanics arising as the high-frequency (rendering) limit of minimising
this single expression.

The spectral action is naturally Euclidean, consistent with the result of
Section~\ref{sec:measure} that the fundamental measure is Euclidean. The
complex structure of quantum mechanics is carried by $D$ itself (not by $D^2$)
and emerges in the Lorentzian continuation, as discussed. The phase cost
$\sum_i \mathrm{Cost}(\phi_i)$ in $\Cs$ corresponds to the eta invariant
$\eta(D) = \sum_i \mathrm{sign}(\lambda_i)|\lambda_i|^{-s}$, which
distinguishes the $\pm\lambda$ eigenvalue pairs of $D$ and carries the
chiral/complex structure absent from the real-valued $\mathrm{Tr}(f(D/\Lambda))$
alone.

\subsection{Why the Dirac operator wins the measure}

Within the \iame{} framework, no geometric encoding is \emph{chosen} by hand.
The induced measure $P(c) \propto 2^{-\Ctotal(c)}$ selects the most compressible
description. The Dirac operator wins because it is the unique minimal sufficient
statistic for Riemannian spin geometry. Any other first-order operator either
fails to capture the full geometry (requiring additional structure, increasing
description length) or is equivalent to $D$ up to gauge transformation
(producing the same spectral action). The Dirac operator is selected not
because it is postulated but because its description is the shortest.

This is the same argument that selects smooth spacetime over chaotic spacetime,
orbiting planets over free particles, and periodic wave-like behaviour over
random jitter: in each case, the compressible structure dominates the measure.

\begin{conjecture}[Dirac spectral rendering]
\label{conj:dirac}
The informational Lagrangian $\Linfo = \Cs + \Cg$ is the scale decomposition
of the spectral action $\mathrm{Tr}(f(D/\Lambda))$, where $D$ is the Dirac
operator on the observer's configuration space. $D$ functions as the natural
rendering engine at the boundary between smooth geometry ($G$) and finite
observer ($D$-particles): real at the geometric level ($D^2$), complex at the
rendering level ($D$ itself). The Dirac operator is the unique minimiser of
description length among all operators encoding spin geometry under the induced
measure $P(c) \propto 2^{-\Ctotal(c)}$. General relativity and quantum
mechanics are respectively the low-frequency (substrate) and high-frequency
(rendering) limits of minimising this single spectral functional.
\end{conjecture}

% ============================================================
\section{Proof of Concept Simulations}
\label{sec:poc}

To test whether the joint compressibility principle can generate realistic
physics without hard-coded laws, a minimal 2+1 dimensional proof-of-concept
simulation was implemented. Observers were defined with a minimal internal
DNA-like rule and grow in complexity configuration by configuration, injecting
new spectral modes frame by frame. No forces, equations of motion, spacetime
metric, or physical laws were programmed. Configurations were sampled using a
Metropolis-Hastings algorithm guided exclusively by minimisation of the total
description length $\Ctotal = \Cs + \Cg$.

Key robustness result: the emergent behaviour is insensitive to the specific
observer rule. Smooth, compressible, law-like universes emerge for every
observer definition tested, including single observers, multiple observers, and
varied DNA-like rules. The smoothness and predictability are outputs of the
measure, not inputs of the observer specification.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{figures/emergent_inertia_gravity.png}
\caption{Emergent inertia and weak mutual attraction.
  Three Gaussian observer proxies evolve under pure minimisation of the joint
  spectral-geometric complexity $\Ctotal = \Cs + \Cg$.
  No equations of motion or forces were programmed.
  The blobs naturally develop persistent trajectories (inertia) and exhibit
  weak attraction toward one another.}
\label{fig:emergent-inertia}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.75\textwidth]{figures/emergent_interference.png}
\caption{Emergent interference patterns. At smaller scales, complex-valued
  wavefunction representations naturally produce interference effects between
  nearby observer proxies, arising solely from the spectral compression term
  $\Cs$ without any explicit wave equation being imposed.}
\label{fig:emergent-interference}
\end{figure}

The simulations demonstrate:

\begin{itemize}
  \item Persistent, approximately inertial trajectories (smooth motion
        compresses better than erratic jitter);
  \item Wave-like interference patterns at small scales;
  \item Weak mutual attraction between observer proxies;
  \item Compression paths that reproduce Euclidean (Wick-rotated) quantum
        paths, confirming that the fundamental measure is Euclidean;
  \item Observer mortality: as the observer accumulates modes (memory), the
        smooth configurations supporting her full structure become rare,
        geometric degradation appears (``wrinkles''), and the observer
        eventually exits the measure-supported region.
\end{itemize}

Full implementation details, source code, parameter settings, convergence
diagnostics, and high-resolution video outputs are provided in the
Supplementary Material.

% ============================================================
\section{Falsifiable Predictions}
\label{sec:predictions}

\textbf{Prediction 1: Dual structure is necessary.}
Any stable observer-hosting physics must exhibit both wave-like ($\psi$) and
geometric ($G$) structure. A universe with only one of the two is forbidden by
the joint compressibility requirement: an observer requires both a geometry to
inhabit and a rendering to be constituted from.

\textbf{Prediction 2: Discreteness is never primitive.}
The framework predicts that any deeper physical theory will not be
fundamentally discrete. Discreteness emerges at the observer boundary as a
rendering artifact. If a future theory demonstrates that fundamental
discreteness is unavoidable, the framework is refuted.

\textbf{Prediction 3: Quantum gravity as spectral rendering minimisation.}
The correct theory of quantum gravity will take the form of a joint
spectral-geometric minimisation principle --- specifically, minimisation of a
Dirac spectral action --- rather than a quantisation of a background metric.
If Conjecture~\ref{conj:dirac} is confirmed, quantum gravity is the full
spectral action without the infrared truncation that yields classical GR.

\textbf{Prediction 4: Low-entropy past without fine-tuning.}
The framework predicts a low-entropy past as the expected outcome of the
compression measure, not a lucky accident. This is a stronger claim than
standard cosmology makes.

\textbf{Prediction 5: Fermionic exclusion from compression.}
The Pauli exclusion principle must emerge naturally from the observer
functional $\muO$ as a consequence of description-length explosion upon
observer merger. If no such mechanism can be identified, the framework is
incomplete.

\textbf{Prediction 6: Macro-micro scaling of interference.}
At macroscopic scales, rendering residuals average out and observers tend
toward smooth classical trajectories. At microscopic scales, the rendering
codec's mode structure is resolvable and wave-like behaviour dominates. This
scaling behaviour should reproduce the quantum-to-classical transition
quantitatively, not merely qualitatively.

\textbf{Prediction 7: Lognormal microstructure distribution.}
The probability of microstructure emergence as a function of distance from a
gravitational singularity follows a universal lognormal profile, independent
of the structural filter and independent of the representational level at which
the analysis is performed.

\textbf{Prediction 8: Quantum fingerprint in gravitational waves.}
Gravitational wave signals from high-energy merger events should exhibit
structured spectral residuals after classical GR waveform subtraction,
reflecting the quantum rendering history of the source. At the moment of
merger, the system crosses through the regime where $\Cs$ and $\Cg$
contributions are comparable, and the rendering layer leaves an imprint on
the outgoing wave. The residuals should be correlated in phase structure
rather than purely stochastic, distinguishing them from instrumental noise.
Classical GR predicts no such structured residual; the \iame{} framework
predicts one whose magnitude is determined by the crossover scale of the joint
measure. This constitutes a quantitative target for future derivation and a
qualitative target for next-generation gravitational wave observatories.

% ============================================================
\section{The Open Problem: Fermionic Exclusion}
\label{sec:open}

The most important open challenge is the natural emergence of fermionic
exclusion statistics from the compression measure.

In the current simulations, Gaussian observer blobs eventually lose their
individual identities and merge. Symmetric (bosonic) configurations are
strongly preferred because they allow shared spectral modes and lower total
description length. Antisymmetric configurations are expensive and suppressed.
Hard-coding antisymmetry would violate the spirit of the project.

We sketch the resolution as follows. Two independent observers $O_1$ and $O_2$
are each separately compressible. When they attempt to occupy the same state,
the merged description must encode both internal structures simultaneously in
the same region. These structures are not correlated --- they were independently
optimised. The result is analogous to injecting frames from one video stream
into another: the combined description length explodes, because the two
rendering schemes interfere destructively.

The exclusion principle then follows not from an external antisymmetry rule but
from the stability-of-identity condition on $\muO$: merger is the least
probable configuration because it maximally violates the observer's
self-consistency requirement. An observer that loses its distinctness ceases
to satisfy the observer boundary condition; $\muO$ returns near zero.

The predicted signature is an effective short-range repulsion sharper and more
localised than the emergent long-range attraction, because attraction arises
from gradual wavefunction overlap (smooth, cheap) while repulsion arises from
structural incompatibility (sharp, expensive). Demonstrating this
computationally in a collision-and-rebound simulation is the current
experimental frontier of the framework.

% ============================================================
\section{Future Work}
\label{sec:future}

Three directions constitute the immediate research frontier of the framework.

\subsection{Rigorous derivation of the Dirac spectral rendering}

Conjecture~\ref{conj:dirac} is the primary mathematical open problem. Its
verification requires two steps.

The first is to establish that the spectral action
$\mathrm{Tr}(f(D/\Lambda))$ decomposes cleanly into $\Cs$ and $\Cg$ at the
appropriate frequency scales. The heat kernel expansion provides the natural
technical tool: the low-mode truncation should recover $\Cg$, and the
high-mode residual should recover $\Cs = \sum_i [\mathrm{Cost}(\phi_i) +
\omega_i/\Delta\omega]$. The identification
$\mathrm{Cost}(\omega_i) \leftrightarrow t\lambda_i^2$ (heat kernel time
$\leftrightarrow$ observer resolution scale) and
$\mathrm{Cost}(\phi_i) \leftrightarrow \eta(D)$ (phase cost $\leftrightarrow$
eta invariant) provides the natural correspondence to verify.

The second is to establish that the Dirac operator is the unique
minimum-description-length rendering engine for spin geometry under the
\iame{} induced measure. This requires a precise formulation of description
length for operators within the \iame{} configuration space and a proof that
$D$ achieves the minimum. This is a problem at the intersection of spectral
geometry, algorithmic information theory, and noncommutative geometry.

\subsection{Robust and representation-independent proof-of-concept simulations}

Three limitations of the current simulations must be addressed. First, more
efficient sampling strategies are needed to obtain results robust against
sampling bias. Second, quantitative agreement with specific numerical
predictions of quantum mechanics and general relativity is the next target,
beyond qualitative emergence. Third, the lognormal microstructure result should
be tested across wider geometries and filter types, and the fixed-point filter
--- whose identification would constitute a derivation of the particle spectrum
from the compression principle --- should be located computationally.

\subsection{Fermionic exclusion from collision-and-rebound simulations}

The immediate experimental target is a simulation of two observer-proxy blobs
on a collision course, in which the expected outcome is rebound with preserved
identity rather than merger. If the sharp short-range repulsion predicted by
the description-length argument emerges naturally, Prediction~5 is confirmed
and the framework's particle physics programme becomes tractable.

% ============================================================
\section{Discussion and Relation to Existing Frameworks}
\label{sec:discussion}

\textbf{Relation to relational quantum mechanics.}
Rovelli's relational quantum mechanics \cite{rovelli1996} similarly holds that
physical quantities are observer-relative. The \iame{} goes further: the
observer and its universe are identical ($O = U_O$), and the relational
structure is derived from a compression measure rather than postulated.

\textbf{Relation to the holographic principle.}
The holographic principle \cite{thooft1993, susskind1995} holds that the
information content of a region is encoded on its boundary. This is consistent
with the \iame{}'s geometric compression scheme $\Cg$, which minimises boundary
description length. The entropy decrease of collapsing spacetime --- reaching
zero at the singularity --- is consistent with the holographic trend.

\textbf{Relation to Barbour's timeless physics.}
Barbour \cite{barbour1999} argues that time is not fundamental and the universe
is a static ``Platonia'' of configurations. The \iame{} reaches the same
conclusion from the opposite direction --- from the observer inward --- and
provides a measure over configurations that Barbour's framework lacks.

\textbf{Relation to Connes' noncommutative geometry.}
Connes' spectral action programme \cite{connes1996} derives the Einstein-Hilbert
action and the Standard Model action from a single spectral triple. The \iame{}
provides the motivation that Connes' approach lacks: the spectral action is
selected because the Dirac operator is the shortest description of the rendering
process between smooth geometry and finite observer. Connes derives the same
functional from different starting axioms; the \iame{} explains why those axioms
hold and gives them physical meaning as the rendering engine of a finite
observer embedded in smooth geometry.

\textbf{Relation to Solomonoff induction and algorithmic information theory.}
The measure $P(c) \propto 2^{-\Ctotal}$ is a variant of the universal prior
of Solomonoff \cite{solomonoff1964}. The \iame{} applies this measure to
observer-conditioned configuration spaces and extends it with an asymmetric
spectral-geometric decomposition motivated by the rendering structure of
observed physics.

\textbf{What is genuinely new.}
The \iame{}'s distinctive contributions are: (1) the derivation of $O = U_O$
from the IEP; (2) the joint spectral-geometric compressibility equation with
its asymmetric rendering interpretation; (3) the identification of the Dirac
operator as the rendering engine rather than a unifier of independent theories;
(4) the Euclidean measure with Lorentzian emergence as the first principled
explanation of Wick rotation; (5) the natural suppression of Boltzmann brains;
(6) the non-epiphenomenal role of qualia; (7) the derivation of observer
mortality from measure thinning; and (8) the prediction of structured quantum
fingerprints in gravitational wave signals.

% ============================================================
\section{Conclusion}
\label{sec:conclusion}

We have presented \iame{}, in which the sole primitive is the existence of a
finite, structured observer, and from which physical laws, spacetime geometry,
quantum behaviour, the arrow of time, the effectiveness of mathematics, the
suppression of disordered fluctuations, and observer mortality all emerge as
derived consequences of a single informational variational principle:

\begin{equation*}
\Linfo = \Cs + \Cg \;\longrightarrow\; \text{minimum}
\end{equation*}

where $\Cg$ is the primary geometric cost and $\Cs$ is the rendering cost of
that geometry at the observer's finite resolution scale. We further conjecture
that this principle reduces to:

\begin{equation*}
\Linfo = \mathrm{Tr}\!\left(f\!\left(\frac{D}{\Lambda}\right)\right)
\;\longrightarrow\; \text{minimum}
\end{equation*}

where $D$ is the Dirac operator, selected by the induced measure as the
unique minimum-description-length rendering engine for spin geometry. In this
picture, quantum mechanics is not a separate theory from general relativity
that must be unified with it. It is what general relativity looks like from
the inside, through a finite rendering engine. The separation was always an
artifact of forgetting that the observer is finite and renders rather than
perceives directly.

\begin{tcolorbox}[title=Summary: \iame{}]
\begin{itemize}[leftmargin=*]
\item Reality is an unrestricted static totality $\mathbf{U}$ containing all
      configurations.
\item Finite observers self-locate in highly compressible structures via
      asymmetric spectral-geometric compression.
\item $G$ is primary: the smooth geometry. $\psi$ is the rendering cost of
      $G$ at the observer's resolution scale. $D$ is the rendered pixel output.
\item Qualia are the felt heuristic guidance toward high-measure structures.
\item Smooth physics, laws, time, and mathematics are high-measure expected
      outcomes, not fine-tuned miracles.
\item The observer and the universe are identical: $O = U_O$.
\item Time has no external clock. The observer is the reading head, finding
      herself through the most compressible continuation of her current state.
\item The fundamental measure is Euclidean. Lorentzian time and complex quantum
      phases emerge via analytic continuation. Wick rotation is the map between
      the rendering level and the experienced level.
\item Cosmic expansion, entropy increase, and the arrow of time are one
      phenomenon seen through three lenses.
\item Gravitational singularities are zero-entropy fixed points: states where
      the rendering has nothing left to render.
\item Observer mortality is a geometric inevitability: memory accumulates
      complexity until the measure can no longer support smooth
      self-description.
\item The Dirac operator is the rendering engine at the boundary between smooth
      geometry and finite observer --- real at the geometric level ($D^2$),
      complex at the rendering level ($D$) --- selected by the compression
      measure as the shortest description of that boundary.
\item Quantum mechanics is what general relativity looks like from the inside,
      through a finite rendering engine. There is nothing to unify.
\end{itemize}
\end{tcolorbox}

% ============================================================
\section*{Acknowledgements}

The author thanks the Internal Emergence Principle for containing this paper
within itself prior to its being written.

\printbibliography

\end{document}
